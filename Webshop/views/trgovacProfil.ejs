<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
  <a href="/logout"><button>Logout</button></a>
    <h1>Korpa</h1>
    <br>
    <br>
    <table class="table">
      <thead>
      <tr>
        <h4>Poruke</h4>
      </tr>
      </thead>
      <tbody>
      <% for (let i=0; i<poruke.length; i++){ %>
        <tr>
          <td><%= poruke[i].poruka%></td>

          <td>
            <form action="/skloniPoruku?id=<%= poruke[i].id %>" method="post">
              <button type="submit">Vidio</button>
            </form>
          </td>
        </tr>
      <% } %>
      </tbody>
    </table>
    <br>
    <div class="d-flex justify-content-center container">
      <div class="korisnickiPodaci">
        <h2><%= korisnik.korisnickoIme%></h2>
        <img src="/images/artikal.jpg" class="img-thumbnail " alt="slika artikla" width="200" height="200">
        <br>

      </div>
      <div class="artikli">
        <br>
        <br>
        <table class="table">
          <thead>
          </thead>
          <tbody>
          <%for (let i=0; i<artikli.length; i++){%>
            <tr>
              <td>
                <img src="/images/artikal.jpg" class="img-thumbnail " alt="slika artikla" width="200" height="200">
              </td>
              <td>
                <h3><%= artikli[i].naziv_artikla%></h3>
                <br>
                <p><%= artikli[i].opis%></p>
              </td>
              <td><%= artikli[i].cijena%></td>
            </tr>
          <%}%>
          </tbody>

        </table>
      </div>
      <div class="korisnickiPodaci">
        <button onclick="location.href='http://localhost:3000/objavaArtiklaUsluge'">Objava artikla/usluge</button>
        <button onclick="location.href='http://localhost:3000/uredjivanjeProfila'">Uredi profil</button>
      </div>
    </div>

  </body>
</html>
